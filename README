skype_oss_wrapper
=================

A fake libpulse.so library that mimics pulseaudio behavior for skype

1.COMPILING:
  PREFIX=/usr/local make
  Replace "/usr/local" with desired destination (defaults to /usr)
  
2.INSTALLING:
  make install
  
3.RUNNING:
  Just run skype_oss
